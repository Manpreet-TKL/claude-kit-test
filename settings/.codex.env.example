# Codex agent defaults, shared by both Codex entry points:
#   - `bash install.sh -x`        the `codex mcp-server` stdio MCP server
#   - `bash codex-install.sh`     the standalone host runner (codex.sh)
#
# Copy to generated/.codex.env and adjust, or just answer the prompts on either
# installer - both write this exact file. Non-interactive runs:
#   bash install.sh -x -p standard -y
#   bash codex-install.sh -q
#
# generated/.codex.env is gitignored (the whole generated/ folder is). This example
# file is safe to commit. NOTE: there is NO secret here - Codex authenticates with
# your ChatGPT login (`codex login`, stored in ~/.codex), not a token in this kit.
#
# The MCP registration bakes these values into CLI overrides. Standalone Codex
# writes model/effort into its kit profile before each fresh launch instead, so
# /model works without override warnings. Resume/fork keep native behavior.
# Explicit CODEX_MODEL/CODEX_REASONING_EFFORT environment values take precedence
# over this file for a standalone launch. Edit + rerun install.sh -x for MCP.

# Default main model; the separate read-only planner uses gpt-6-astra at max.
CODEX_MODEL=gpt-6-astra

# Reasoning effort: use a level offered by /model (support depends on model).
CODEX_REASONING_EFFORT=xhigh

# Versioned permission tier and session-mode names used by codex-install.sh.
CODEX_PERMISSION_TIER=standard
CODEX_MODE=auto

# Maximum open native subagent threads per Codex session. Blank uses Codex's
# built-in default. Changes apply to new sessions.
CODEX_AGENT_THREADS=
