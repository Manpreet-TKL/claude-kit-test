---
name: nodaudit-validation-snail
description: NodAudit module validated on snail vs legacy NOD Export; IMD lookup rewrite applied (uncommitted); full run 10-17s
metadata: 
  node_type: memory
  type: project
  originSessionId: 4351dbb7-8f3b-45a8-a9c2-5484e312038a
  modified: 2026-08-07T18:31:30.948Z
---

NodAudit special module (host repo `/home/toukan/Temp10/nodaudit-test`, container `/var/www/openeyes/protected/modules/NodAudit` on snail-web-1) validated 2026-08-07 against the legacy baseline `~/RMC_202607241624_NOD_Export.zip` (Cataract 2025-04-01..2026-03-31), which was generated by `~/NodExportv26Command.php` - a console hotfix port of `NodExportController` (the hotfix deleted IMD, dropped ORDER BYs on the 3 biggest files, and drops right-eye-only refractions via a `sr.eye_id = 1` copy-paste).

- Runs sat "queued" because snail has no `yiic nodauditworker` cron; manual trigger: `docker exec snail-web-1 php /var/www/openeyes/protected/yiic.php nodauditworker`.
- IMD postcode lookup rewritten in `components/datasets/CataractDataset.php` + `AmdDataset.php`: correlated `IN (a.postcode, REPLACE(...))` replaced by two COALESCE'd single-equality probes with `CONVERT(... USING latin1)` (see [[mariadb-correlated-in-list-no-index]]). Full Cataract run: 17s then 10s; repeat runs byte-identical. Full-cohort equivalence proven: old vs new form identical ltim row id for 1,013/1,013 patients, 0 mismatches (old form took 45.5 min, new <1s). **Edits are uncommitted in the host repo** (was clean at 8a286c2); container copy patched via docker cp.
- Validation: new output = strict subset of old with zero orphan keys; every old-vs-new delta traced to a legacy bug (right-eye biometry = left values, PostOpComplication OperationId = exam id, cohort includes non-cataract ops, unlimited history, empty CVI, un-correlated PreOp risk flags, IMD never populated). Full writeup: `/home/toukan/nod-audit-validation.md`.
- NodAudit hmac31 pseudonyms: PHP `hash_hmac` uses the 64-char hex key string as raw ASCII bytes (do NOT hex-decode when reproducing in python); spaces `P:`/`S:`/`E:`/`T:`, big-endian first 4 bytes & 0x7FFFFFFF.
- Test runs 1-4 left in snail `nod_audit_run` (2 failed from the pre-fix attempt, 3-4 complete).
