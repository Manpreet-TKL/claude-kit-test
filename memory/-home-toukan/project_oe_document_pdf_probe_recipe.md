---
name: oe-document-pdf-probe-recipe
description: OE Document+PDF repro loadout — the verified recipe lives in the c-oe-nav atlas; /c-oe-repro-kit loads the whole loadout in one command
metadata: 
  node_type: memory
  type: project
  originSessionId: 4b42f26e-2705-4bb1-a004-aa32ecb69804
---

The zero-discovery recipe for driving a Document-event PDF upload + page-preview generation (form selectors, gs test-PDF one-liner, `getImageInfo` trigger, `/tmp/oe_pdf*` leak check) is recorded in the c-oe-nav atlas — `~/.claude/skills/c-oe-nav/subs/paths.md`, "Document create form" section (verified live v11.0.18, 2026-07). The journey driver's full action schema is inline in `subs/probe.md` — never read `journey.mjs` for it. `/c-oe-repro` is the single entry point — `c-oe-repro-kit` was removed 2026-07-03 at Manpreet's request ("I only want c-oe-repro"; SKILL.md backed up in `~/.claude-backups/20260703-repro-kit-removed/`), and c-oe-repro now lives in `~/claude-kit/skills/` (symlinked, kit conventions apply). Companion skills (c-oe-nav, c-dblogin, c-oe-code) are loaded by Reading their SKILL.md files directly — c-* context skills reject the Skill tool (`disable-model-invocation`), and SKILL.md bodies do not expand @file refs (confirmed against docs 2026-07). Probe pattern that worked: pin selectors/ids in the main context first, then one Haiku subagent finishes the walk in ~3 driver runs / ~30k tokens. Re-verified end-to-end 2026-07-03 on snail-web-1 (2-page PDF → +2 stubs): the exact paste-ready procedure (both OE_ACTIONS lists, gs one-liner, patched-check grep) is now canned in `~/.claude/skills/c-oe-nav/subs/canned-document-pdf.md` — hand that file to a Haiku subagent verbatim; skip reading probe.md/paths.md for this walk. Token rules learned: (1) check patched/unpatched via in-container grep of BaseEventTypeController before walking — it predicts the outcome; (2) if the PR.md Test section already documents a live before/after walk from a prior session, a fresh walk is redundant unless the user asks for one. Relates to [[project_oe_eventimage_docman_render_testing]].
