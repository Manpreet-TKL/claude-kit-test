- [Default-on-create roles](feedback_default_on_create_roles.md) - new ToukanNotes roles should default-tick on user creation; the roles table carries a `default_on_create` flag (c-notes-app skill records the mechanism).
- [Keep ~/Temp10 synced](feedback_keep_temp10_synced.md) - proactively sync ~/Temp10/<module>-test <-> container on every change, don't wait to be asked; prefer docker cp for host->container, `bash ~/Temp10/copyin.sh out` for container->host (script + local_common.php live in ~/Temp10 since 2026-08-09).
- [oe-deploy conventions](feedback_oe_deploy_conventions.md) - `mv` (never `cp`) the template; hosts pre-provisioned (skip host-setup/docker-login/GPG); sample DB is `admin`/`admin`; lowercase rc image tags (`v26.0.0-rc3`).
- [OE module config](feedback_oe_module_config.md) - module declares itself in `<module>/config/common.php`; `local/common.php` is only the on-switch, patched in-container directly; never edit `core/common.php`.
- [voiceControl independent of aiSearch](feedback_voice_control_independent_of_aisearch.md) - voiceControl module (~/Temp10/oevoicecontrol-test) must stand on its own; no runtime dependency on or integration with aiSearch.
- [House style -> user skill](feedback_house_style_as_skills.md) - capture style guides (bash, yiic, notes, ...) as skills in `~/claude-kit/skills/<name>/` + symlink into `~/.claude/skills` (never a real dir there), not project CLAUDE.md or memory.
- [Double-letter command naming](feedback_double_letter_naming.md) - double the trailing letter when the name is an English word or existing Unix command (`helpp`, `pss`, `versionss`, `secss`); leave compound/coined names alone.
- [Codex per-task only](feedback_codex_per_task_delegation.md) - no standing "prefer codex" rule; delegate to codex MCP agents only on explicit ask; codex-grill/codex-swarm confirm before spawning.
- [Traefik <3.6 min-api fix](feedback_traefik_min_api_fix.md) - Traefik <v3.6 (oe-deploy still defaults v3.3) needs the Docker daemon's `min-api-version` lowered to `1.24` in `/etc/docker/daemon.json` (host write - Manpreet applies). Don't swap Traefik tag or add a socket proxy.
- [PR base is the mainline (non-OE)](feedback_pr_target_main.md) - create-pr patches cut against the mainline (main on newer repos, master on older) unless Manpreet names another base; OE repos follow create-oe-pr develop/release rules.
- [Bulk Jira download = bash script, not MCP](feedback_bulk_jira_download_bash_not_mcp.md) - bulk ticket exports via `~/claude-kit/scripts/jira_filter_download.sh` (REST, zero model tokens, `-r` resumes); MCP-agent fetch-and-transcribe burned ~400k tokens on 2.4 MB; MCP stays for interactive triage only.
- [claude-kit remote is public](project_claude_kit_public_remote.md) - treat every tracked file as published; secrets live in `~/.claude/mcp-env` + `~/.claude/oe-chrome-agent`; the Mirth/client material in the kit is Manpreet's to handle - report, never edit.
- [Codex docker swarm gotchas](project_codex_docker_swarm_gotchas.md) - codex MCP calls in the kit container need per-call `sandbox: "danger-full-access"` (bwrap can't nest) and a local `AGENTS.md` in cwd, else agents invoke kit skills (jiramcp) and stall on the gate; 1800s idle timeout kills long slices.
- [Mirth channel corpus analysis](project_mirth_channel_corpus_analysis.md) - 13-instance BridgeLink corpus (moved out of the kit to ~/mirth-channel-corpus); 16-phase analysis/templating/deploy brief; hybrid Claude+Codex(luna/terra) swarm; PASAPI V1<=OE8/V2=OE9-10/V3=OE11+.
- [TKLS full corpus downloaded + analysis queued](project_tkls_full_corpus_download_analysis.md) - 9,940 tickets + attachments (22 GB) complete in ~/tkls-corpus-full; analysis brief in kit todo/, stats gate before any model pass.
- [cleardown / pptest campaign](project_cleardown_pptest_campaign.md) - battery PASSED 2026-08-02, d walk + e smoke PASSED 2026-08-03 (8 S leanings, 2 ensure floors); v26.1 seed at ~/pptest-seed-v26-1-template-config.sql; open: leanings review, battery re-run, seed-regen decision; oe-checkout aborts on dirty modules (stash unblocks); `cleardown report` takes no profile flags.
- [mc entrypoint '=' password bug](project_mc_entrypoint_equals_password_bug.md) - mc entrypoint's mirth.properties merge strips trailing '=' from passwords; rotate_secrets.sh strips padding, environment-setup.sh -ndp still latent (verified 2026-08-19); real fix belongs in the OEImageBuilder entrypoint.
- [monkey environment](project_monkey_environment.md) - remote browserless chrome rendering (always `docker compose -f docker-compose.yml`; deletions revert on recreate), HTTP-only Traefik PathPrefix routing on `:80`, OpenMRS first boot ~73 min (legacy UI `/openmrs/login.htm`).
- [snail DB layout](project_snail_db_layout.md) - since 2026-08-10 snail `openeyes` = official sample DB (admin/admin); keeper dataset parked intact at `openeyes2` (421,849 patients); DDL backup snail-db-1:/var/tmp/oe-park.
- [NodAudit validation on snail](project_nodaudit_validation_snail.md) - module validated vs legacy export 2026-08-07; IMD lookup rewrite committed (743f086) in ~/Temp10/nodaudit-test; run 10-17s; hmac31 key = hex string as ASCII; writeup in kit `knowledge/oe-nodaudit-validation.md`.
- [OE v26 resetuserlock NULL-institution bug](project_oe_v26_resetuserlock_null_institution.md) - `set_frontend_passwords.sh`/`yiic resetuserlock` cannot reset users whose auth row has NULL `institution_authentication_id`; reset via model pipeline with `save(false)`.
- [OEImageBuilder build gotchas](project_oeimagebuilder_build_gotchas.md) - openeyes repos are private: builds need `--ssh default=/tmp/oe-build.agent` (in-session ssh-agent) + `--platform linux/amd64`; node 24.16 regression resolved (archived in knowledge/).
- [Host OE checkout lags the container](project_oe_host_checkout_lags_container.md) - `~/openeyes` on the host drifts days behind the running web container (and the DB carries migrations it lacks); read app source via `docker exec`, not the host clone. Also: `docker cp` cannot read the container `/tmp` (tmpfs), and Node scripts in `/tmp` cannot resolve `require('puppeteer')`.
- [OE docs screenshot backlog not fan-out-able](project_oe_docs_screenshot_backlog_not_fanoutable.md) - capture.cjs is direct-URI+selector only (no clicks/writes); residual 56 of 710 (13 = review/ routes that 500/400, 43 serial/manual); canonical backlog IN-MODULE at docs/help/screenshot-backlog.md (recipes inlined, reconciled 2026-08-10).
- [OE Document+PDF probe recipe](project_oe_document_pdf_probe_recipe.md) - `/c-oe-repro` is the sole entry; paste-ready walk canned in c-oe-nav `subs/canned/document-pdf.md` (hand to a Haiku subagent verbatim); grep the container for patched/unpatched before walking.
- [oe-frontend-tests repo](project_oe_frontend_tests_repo.md) - `~/oe-frontend-tests` (ex oe-sitemap): end-user FRONTEND Playwright suite - `admin` + `app` projects, the OeDocumentation seed suite (`tests/seed`, 30 specs green + idempotent 2026-08-10, 29-token manifest on all-green), two images (`:run`/`:author`); OE gotchas - no `networkidle`, JS row-click lists, SSO all-OIDC-required, Yii lowercase redirects, BUG-531 dropdown workaround.
- [OE end-user Playwright harness (planned)](project_oe_playwright_enduser_harness.md) - standalone UI-only harness (CDP chromium sidecar, contexts-only load, one artifacts volume, runtime version gate); plan approved 2026-06-25 but NOT started - Manpreet must supply stack AND target env; plan at kit `todo/oe-playwright-harness-plan.md`.
- [screen 5 TERM/prompt gotchas](project_screen5_term_prompt_gotchas.md) - defbce makes TERM=screen-256color-bce (kills colour prompt); screen mangles emoji+VS16 into `_`; detached screen tests show TERM=unknown; Claude Code copy leaks OSC 52 base64 into the prompt (copy outside screen or base64 -d the blob).
- [Claude Code MCP config location](reference_claude_code_mcp_config_location.md) - MCP servers load from ~/.claude.json (claude mcp add -s user) or .mcp.json, NOT settings.json; tools bind at startup; `claude mcp get` launches the server (consumes the kit's gate flag).
- [OE render testing gotchas](reference_oe_render_testing_gotchas.md) - on sample containers `yiic eventimage` fails silently (docman auth): drive renders via a direct admin web session with institution_id+site_id; a relocated Puppeteer temporaryDirectory must be mkdir'd in .puppeteerrc.cjs or every render 500s.
- [One unbooked worklist per day](reference_oe_unbooked_worklist_first_event_claims.md) - the day's FIRST saved event claims the patient for its subspecialty's unbooked list; later same-day saves add nothing, so probes/verification must span ALL of today's unbooked lists.
- [MariaDB correlated IN-list kills index](reference_mariadb_correlated_in_list_no_index.md) - col IN (outer_ref, outer_ref) never uses an index; rewrite as COALESCE'd single-equality probes + CONVERT the outer side on cross-charset compares.
