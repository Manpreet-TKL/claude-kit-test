- [Chat tone](feedback_roadman_tone.md) — delightful, warm, plain English in chat; roadman slang explicitly revoked 2026-06-09. Deliverables stay professional.
- [Default-on-create roles](feedback_default_on_create_roles.md) — new ToukanNotes roles should default-tick on user creation; the roles table carries a `default_on_create` flag.
- [oe-deploy host pre-provisioning](feedback_oe_deploy_host_provisioning.md) — Docker login, GPG, host-setup are already done on Manpreet's hosts; don't re-check or re-run.
- [oe-deploy template rename](feedback_oe_deploy_template_rename.md) — `mv oe-deploy <env>`, never `cp`. The repo is designed as a template, not a source-of-truth checkout.
- [OE sample DB admin creds](feedback_oe_sample_db_admin_creds.md) — seeded OE ships with `admin`/`admin`; don't run `set_frontend_passwords.sh` on demos.
- [OE module config layering](feedback_oe_module_config_layering.md) — module advertises itself in `<module>/config/common.php`; `local/common.php` is just the on-switch; never edit `core/common.php` from a module install.
- [voiceControl independent of aiSearch](feedback_voice_control_independent_of_aisearch.md) — voiceControl module must stand on its own; no runtime dependency on or integration with aiSearch.
- [No local/common.php template in module](feedback_no_local_common_template_in_module.md) — patch in-container `local/common.php` directly; don't keep a host-side template copy inside the module.
- [House style → user skill](feedback_house_style_as_skills.md) — capture style guides (bash, yiic, notes, …) as skills in `~/claude-kit/skills/<name>/` + symlink into `~/.claude/skills` (never a real dir there), not project CLAUDE.md or memory.
- [Double-letter command naming](feedback_double_letter_naming.md) — double the trailing letter when the name is an English word or existing Unix command (`helpp`, `pss`, `versionss`, `secss`); leave compound/coined names alone.
- [One-line CLI commands](feedback_one_line_cli_commands.md) — runnable commands on ONE line, up to 200 chars; never backslash-wrapped.
- [Codex per-task only](feedback_codex_per_task_delegation.md) — no standing "prefer codex" rule; delegate to codex MCP agents only on explicit ask (suggest at most for big parallelisable jobs).
- [OE web/manager RC tag casing](feedback_oe_web_lowercase_rc.md) — `OE_WEB_TAG`/`MASTER_TAG` need lowercase rc suffix (`v26.0.0-rc3`); Docker Hub is case-sensitive. BridgeLink has no bare `4.6` — pin a patch like `4.6.1`.
- [OpenMRS first-boot is slow](project_openmrs_first_boot_long.md) — O3 ref-app backend took ~73 min on first WAR deploy on monkey; don't quote "5–10 min". Legacy UI is at `/openmrs/login.htm`, bare `/openmrs/` returns `{}` from the REST root.
- [Traefik <3.6 min-api fix](feedback_traefik_min_api_fix.md) — Traefik <v3.6 needs the Docker daemon's `min-api-version` lowered to `1.24` in `/etc/docker/daemon.json`; PR's `minApiFix()` does it. Don't swap Traefik tag or add a socket proxy.
- [monkey Traefik HTTP-only routing](project_oe_monkey_traefik_http_only.md) — monkey runs Traefik HTTP-only on `:80`, PathPrefix routing: `/` → OE, `/openmrs/spa` → SPA (strip), `/openmrs` → backend. No DNS, no TLS.
- [monkey remote chrome](project_monkey_remote_chrome.md) — rendering is pure HTTP to a browserless `chrome` service (no node/puppeteer in app containers, deletions revert on recreate); always `docker compose -f docker-compose.yml` (override.yml is a dev-swap landmine); docman_user fixed 2026-06-09.
- [OE v26 resetuserlock NULL-institution bug](project_oe_v26_resetuserlock_null_institution.md) — `set_frontend_passwords.sh`/`yiic resetuserlock` cannot reset users whose auth row has NULL `institution_authentication_id`; reset via model pipeline with `save(false)`.
- [Node 24.16.0 puppeteer regression](project_node_2416_puppeteer_regression.md) — 24.16.0 silently kills puppeteer's Chrome unzip (exit 0, partial folder); Web-Live pins node 24.15.0, unpin at 24.17.0 (nodejs/node#63487).
- [openeyes repos are private; SSH builds](project_openeyes_repos_private_ssh_build.md) — anonymous access 401s, host git only works via VS Code askpass; builds need `--ssh default=/tmp/oe-build.agent` (user starts agent) + `--platform linux/amd64`.
- [cat: empty DATABASE_SSL_CA breaks Laravel](project_cat_database_ssl_ca_empty.md) — 0-byte secret + `MYSQL_ATTR_SSL_CA` env = Laravel DB SSL error loop; config cached at startup; Yii unaffected so containers stay "healthy".
- [Atlassian scoped-token gateway](project_atlassian_scoped_token_gateway.md) — scoped API tokens 401 on the site URL; mcp-atlassian needs a classic unscoped token. TKLS lives on openeyes; cloudId recorded.
- [Claude Code MCP config location](reference_claude_code_mcp_config_location.md) — MCP servers load from ~/.claude.json (claude mcp add -s user) or .mcp.json, NOT settings.json; tools bind at startup.
- [Puppeteer temporaryDirectory mkdtemp](reference_oe_puppeteer_temporarydirectory_mkdtemp.md) — relocating .puppeteerrc.cjs temporaryDirectory 500s every render unless the dir is mkdir'd (mkdtemp won't create parents); static checks miss it.
- [OE eventimage docman render testing](project_oe_eventimage_docman_render_testing.md) — yiic eventimage fails silently on sample containers (docman auth); drive renders via direct admin web session with institution_id+site_id.
- [oe-frontend-tests repo purpose](project_oe_frontend_tests_repo_purpose.md) — `/home/toukan/oe-frontend-tests` (renamed from `oe-sitemap`) is a whole-app end-user FRONTEND Playwright suite: `admin` project (267+33 specs) + `app` project (80-page reachability + gesture specs); 62 areas/390 pages mapped; two images (`:run`/`:author`); OE gotchas — no `networkidle`, list rows open via JS row-click.
- [OE Document+PDF probe recipe](project_oe_document_pdf_probe_recipe.md) — paste-ready walk canned in c-oe-nav `subs/canned-document-pdf.md` (hand to a Haiku subagent verbatim; skip probe.md/paths.md); `/c-oe-repro` is the sole entry (repro-kit removed 2026-07-03; Read companion SKILL.mds directly); grep the container for patched/unpatched before walking.
- [OE end-user Playwright harness](project_oe_playwright_enduser_harness.md) — new standalone repo mimics end-user journeys (UI-only, CDP chromium sidecar, contexts-only load, one artifacts volume, runtime version gate); independent of OpenEyes/playwright (which verifies page data); plan approved, but stack AND target env both TBD (user supplies); plan at ~/oe-playwright-harness-plan.md.
- [OE docs screenshot backlog not fan-out-able](project_oe_docs_screenshot_backlog_not_fanoutable.md) — capture.cjs is direct-URI+selector only (no clicks/writes); residual 93 shots gated on serial event-create/BUG-042/42-unpopulated-modules; canonical backlog IN-MODULE at docs/help/screenshot-backlog.md (recipes inlined).
