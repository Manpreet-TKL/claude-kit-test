# create-pr - the PR.md form, field rules, and gotchas

## PR.md shape

`PR.md` is a short form with exactly three labelled fields - no blockquote ceremony:

```
Apply onto:
<base branch the diff was cut from + sha, e.g. main @ a1b2c3d - the far side applies
`changes.diff` onto its current base with `git apply --3way`>

Branch:
<branch name - must start `fix/` (defect) or `feature/` (new behaviour), then a short
slug; e.g. fix/archived-notes-reappear>

Commit title(s):
<suggested `git commit -m` message(s) for you to author on the far side - the skill never
commits. One commit by default; add a block only when a split is genuinely beneficial. Each
commit self-contained and green. The headline commit summarises the change.>

    <suggested commit message>

Description:
<a short what-and-why paragraph: the user-visible behaviour or the gap it addresses,
and the approach in one or two sentences. Plain language, no client names. Mark new
files (new) and regenerated artifacts (incidental) if it helps the reviewer, and name
the test added (or why none).>
```

Keep it short; never pad. If a fault can't be pinned down cleanly (intermittent, data-dependent), say what you can - don't fabricate.

## Multiple commits

**Default to one commit.** Split into multiple only when genuinely beneficial - independent
concerns that each stand alone and stay green. Never split by file type or for tidiness. When
you do split, list one suggested title per commit and say which files belong to each, so you
can stage `changes.diff` accordingly when you apply it; you author each real message yourself.
Never split a fix from its proving test; never a commit that won't build alone.

## Building the diff (how the skill assembles the folder)

The deliverable is a single `changes.diff`, not a clone - tiny to copy away and never pinned
to a stale base (the far side applies it onto whatever the base is *now*).

- **Base:** resolve the target base branch (the repo's default branch, or the one the user
  names). Record it in `PR.md` as `Apply onto: <branch> @ <sha>`.
- **Produce the diff:** generate one unified `git diff` against that base from a checkout that
  has the change - normally the working copy where you made it (`git -C <wc> diff <base>`,
  fetching the base ref first if absent), or a throwaway clone used only to build it. Only
  `changes.diff` is kept; the checkout is not part of the deliverable. It captures additions,
  edits, and deletions as content - new files show `new file`, deletions `deleted file`. It
  carries no commit message.
- **Applying it (on the far side, in your live checkout):** `git checkout -b <branch>
  <current-base>` then `git apply --3way changes.diff` (a moved base degrades to conflict
  markers, not a reject). Then write each commit yourself from the suggested title and push.
  The skill never commits or pushes.

## Field rules

- **Branch prefix:** `fix/` for a defect, `feature/` for new behaviour - nothing else.
- **Lockfiles and generated artifacts** regenerated by the change are included in `changes.diff` too, noted (incidental) in the Description.

## Gotchas to catch before finalising

- **Secrets sweep** - no credentials, tokens, API keys, or internal hostnames anywhere in `PR.md` or `changes.diff`.
- **CI-generated files** - if a file is regenerated by CI (README from a template, lockfiles, build output), edit the source and say so; don't hand-edit the output.
